setup:
    npm ci --omit=dev
    npx vite build

setup-dev:
    npm install --include=dev

format:
    npx eslint --fix .

test:
    npx eslint .
    npx vite build
    node --test

dev:
    npx vite --host
